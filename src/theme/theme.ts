export interface Theme {
  background: string
  header: {
    background: string
    color: string
    size: string;
  }
}

export const theme = {
  background: "#202124",
  header: {
    background: "#454545",
    color: "#FFFFFF",
    size: "26px"
  }
}
